"""Small shared constants, kept here to avoid import cycles between the
definition layer and the feature layer."""

from __future__ import annotations

#: How a definition addresses its own entity's table.
SELF_NS = "self"
